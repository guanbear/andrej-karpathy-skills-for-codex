# v0.1.1

Small installation polish release.

## Added

- Adds `scripts/install-local-skill.sh` for one-command local Codex skill installation.
- Updates English and Simplified Chinese READMEs to prefer the installer.
- Extends validation to require the installer script.

## Verified

- Fresh clone from GitHub succeeds.
- `node scripts/validate.mjs` passes.
- Manual README install path works in a temporary HOME.
- Installer symlink path resolves to a complete skill containing `SKILL.md` and `agents/openai.yaml`.
